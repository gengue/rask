{
  "name": "@rask/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "measure": "bun --env-file=../../.env scripts/measure-sync.ts",
    "start:local": "bun --env-file=../../.env src/index.ts"
  },
  "dependencies": {
    "@rask/clickup-client": "workspace:*",
    "@rask/schema": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "zod": "^4.4.3"
  }
}
