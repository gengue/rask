{
  "name": "@rask/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "seed": "bun --env-file=../../.env scripts/seed-dev.ts",
    "start:local": "bun --env-file=../../.env src/index.ts"
  },
  "dependencies": {
    "@rask/clickup-client": "workspace:*",
    "@rask/schema": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.3",
    "zod": "^4.4.3"
  }
}
